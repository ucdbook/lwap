var fs = require('fs');
var path = require('path');

module.exports = {
    masterAppNavsFile: path.join(__dirname, '../../master_app_navs_config.json'),
    childAppFile: path.join(__dirname, '../../child_app_config.json'),
}